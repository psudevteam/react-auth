const ContentLayout = ({ children }) => {
  return (
    <div className="flex flex-col w-full h-full items-center justify-center mt-16 ">
      {children}
    </div>
  );
};

export default ContentLayout;
